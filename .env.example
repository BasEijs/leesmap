# --- De Correspondent access (you must be a paying member) ---

# Your personal RSS feed URL (all publications). Get it from
# https://decorrespondent.nl/rss while logged in — copy the "alle publicaties" link.
DC_RSS_URL=https://decorrespondent.nl/rss/...your-personal-feed...

# --- Login (preferred) ---
# Your De Correspondent email + password. The app logs in for you, caches the
# session on the /data volume, and re-logs-in automatically when it expires — no
# more hand-feeding cookies. Keep these secret; never commit your real .env.
DC_EMAIL=you@example.com
DC_PASSWORD=your_password

# --- Manual cookie (fallback) ---
# Optional. If set, it OVERRIDES the email login above and is used as-is (the old
# behaviour). Leave blank to use DC_EMAIL/DC_PASSWORD. To get it: in a browser on
# decorrespondent.nl, DevTools > Network > any request > Request Headers > copy
# the whole "Cookie:" value. A hand-fed cookie is NOT auto-refreshed.
DC_COOKIE=

# --- Reader ---
# CrossPoint on its own hotspot answers at 192.168.4.1 (or crosspoint.local).
# If the reader joined your home WiFi, use the IP your router gave it.
X4_IP=192.168.4.1

# --- Optional: protect the web UI with HTTP basic auth ---
# Leave blank if it's already private (e.g. behind Tailscale).
BASIC_AUTH_USER=
BASIC_AUTH_PASS=

# --- Optional: admin password for settings + send/publish ---
# Narrower than BASIC_AUTH_*: gates the "instellingen" drawer and the "Extra
# opties" actions (Verstuur naar X4 / Publiceer naar OPDS), while browsing and
# Download .epub stay open. Handy if you share the app (e.g. with family) and
# want to stop them from reconfiguring it or triggering a send by accident.
# Leave blank to disable the gate entirely.
ADMIN_PASSWORD=

# --- Optional: Calibre-Web (publish books from your library to OPDS) ---
# Point this at your Calibre-Web root (NOT including /opds). Leesmap browses
# its OPDS feed and, when you pick a book, copies the EPUB onto its own
# "Gepubliceerd — Calibre-Web" OPDS shelf so the reader/phone can pull it.
# Leave CALIBRE_WEB_URL blank to hide the Calibre-Web view entirely.
CALIBRE_WEB_URL=https://books.example.com
# Only needed if your OPDS feed requires a login (i.e. anonymous browsing is
# off). Sent as HTTP Basic Auth. Leave blank if /opds is publicly readable.
CALIBRE_WEB_USER=
CALIBRE_WEB_PASS=

# --- Optional: Send-to-PocketBook (e.g. a PocketBook Verse Pro) ---
# Mails the same nightly digest EPUB already published to OPDS to your
# PocketBook device's own address, as a second delivery channel. Unlike the
# X4's OPDS pull, PocketBook Cloud downloads mail sent here automatically once
# the device has WiFi — no on-device browsing step.
#
# 1. On the device: Settings > Accounts and Synchronization >
#    Send-to-PocketBook > register with a contact email, follow the
#    activation link it emails you, then copy the resulting
#    username@pbsync.com address below.
# 2. Fill in SMTP_* with a mail account/relay leesmap can send through.
# 3. Set SMTP_FROM to the SAME contact email you registered with in step 1
#    (PocketBook only accepts mail from a white-listed sender; anything else
#    gets a one-time confirmation email instead of delivering the file —
#    approve that once from your device/inbox if you use a different FROM).
# Leave POCKETBOOK_EMAIL blank to skip this channel; OPDS keeps working either way.
POCKETBOOK_EMAIL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# --- Optional: second Send-to-PocketBook device (e.g. Marieke's reader) ---
# PocketBook ties the registered contact/sender address to your account, so
# it can't be reused for a second registration — you need a genuinely
# different sender identity. Cheapest fix: a second Gmail mailbox used only
# as that sender (no separate hosting/relay needed).
#
# 1. On Marieke's device: Settings > Accounts and Synchronization >
#    Send-to-PocketBook > register with the SECOND Gmail address below,
#    follow the activation link, then copy the resulting
#    username@pbsync.com address into POCKETBOOK_EMAIL_MARIEKE.
# 2. In that second Gmail account: Google Account > Security > 2-Step
#    Verification > App passwords > generate one for "Mail" — use it as
#    SMTP_PASS_MARIEKE (Gmail SMTP requires an app password, not your normal
#    login password, once 2FA is on).
# 3. SMTP_HOST/SMTP_PORT above are reused as-is (same Gmail SMTP server for
#    both mailboxes); only the user/pass/from differ here.
# Leave POCKETBOOK_EMAIL_MARIEKE blank to skip this channel entirely.
POCKETBOOK_EMAIL_MARIEKE=
SMTP_USER_MARIEKE=
SMTP_PASS_MARIEKE=
SMTP_FROM_MARIEKE=

# --- Optional ---
# PORT=8080
# USER_AGENT=
